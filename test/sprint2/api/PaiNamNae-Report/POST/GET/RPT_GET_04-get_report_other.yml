info:
  name: RPT_GET_04-get_report_other
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/reports/{{report_id}}"
  auth:
    type: bearer
    token: "{{passenger_02_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Unauthorized access", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
