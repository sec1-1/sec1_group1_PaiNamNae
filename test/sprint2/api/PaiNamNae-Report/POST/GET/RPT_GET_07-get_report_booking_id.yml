info:
  name: RPT_GET_07-get_report_booking_id
  type: http
  seq: 7

http:
  method: GET
  url: "{{base_url}}/api/reports/booking/{{bookingId}}"
  auth:
    type: bearer
    token: "{{passenger_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code ต้องเป็น 200 หรือ 201", () => {
          expect([200, 201]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
