info:
  name: RPT_GET_03-get_report_unauthorized
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/reports/{{report_id}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Unauthorized access", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
