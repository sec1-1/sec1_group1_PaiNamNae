info:
  name: RPT_GET_06-admin_get_report
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/reports/admin/{{report_id}}"
  auth:
    type: bearer
    token: "{{admin_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code ต้องเป็น 200 หรือ 201", () => {
          expect([200, 201]).to.include(res.getStatus());
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
