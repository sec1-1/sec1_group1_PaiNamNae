info:
  name: Passenger Report
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/reports"
  body:
    type: multipart-form
    data:
      - name: type
        type: text
        value: PASSENGER
      - name: category
        type: text
        value: PAYMENT_ISSUE
      - name: routeId
        type: text
        value: "{{routeId}}"
      - name: bookingId
        type: text
        value: "{{bookingId}}"
      - name: targetUserId
        type: text
        value: "{{driver_id}}"
      - name: images
        type: file
        value:
          - /home/saran/Pictures/kku/soften/memeMan.jpeg
      - name: videos
        type: file
        value:
          - /home/saran/Videos/labExamSoftDev/Postman/putOrder-2025-10-18_23.07.49.mp4
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        //counter for different report content
        let counter = bru.getEnvVar("counter") || 0;
        counter++;
        bru.setEnvVar("counter", counter);
    - type: tests
      code: |-
        const body = res.getBody();
        const report = typeof body === "string" ? JSON.parse(body) : body;

        test("Status code ต้องเป็น 400", () => {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
