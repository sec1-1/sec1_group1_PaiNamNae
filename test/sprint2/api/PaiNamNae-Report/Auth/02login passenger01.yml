info:
  name: 02login passenger01
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "username": "{{passenger01_username}}",
        "password": "{{passenger01_password}}"
      }

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        const json = typeof body === "string" ? JSON.parse(body) : body;
        bru.setEnvVar("passenger_token", json.data.token);
        bru.setEnvVar("passenger_id", json.data.user.id);
    - type: tests
      code: |-
        const body = res.getBody();
        const json = typeof body === "string" ? JSON.parse(body) : body;

        test("Status code: 200", () => {
          expect(res.getStatus()).to.equal(200);
        });

        test("คืนค่า token กลับมา", () => {
          expect(json.data.token).to.be.a("string");
        });

        test("ส่ง role: passenger กลับมา", () => {
          expect(json.data.user.role).to.equal('PASSENGER');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
