info:
  name: Auth
  type: folder
  seq: 1

request:
  auth: inherit

docs:
  content: |
    When use command bru.setEnvVar(); to collect the user login token , the variable won't show but the token itself still works.

    Driver01 has review
    Driver02 dont have review
  type: text/markdown
