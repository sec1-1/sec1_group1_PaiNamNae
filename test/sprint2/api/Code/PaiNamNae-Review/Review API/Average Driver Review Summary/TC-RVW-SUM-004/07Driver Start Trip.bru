meta {
  name: 07Driver Start Trip
  type: http
  seq: 5
}

patch {
  url: {{base_url}}/api/routes/{{routeId}}/start
  body: json
  auth: bearer
}

auth:bearer {
  token: {{driver2_token}}
}

tests {
  test("Start trip: status 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
