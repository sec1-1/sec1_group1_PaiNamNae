info:
  name: driver01 vihecle id
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/vehicles"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{driver_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        const body = res.getBody();
        const json = typeof body === "string" ? JSON.parse(body) : body;

        bru.setEnvVar("vehicleId", json.data[0].id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
