info:
  name: TC-DRIVER-002-driver-report-empty-description
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}reports"
  body:
    type: multipart-form
    data:
      - name: type
        type: text
        value: DRIVER
      - name: category
        type: text
        value: OTHER
      - name: description
        type: text
        value: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("ต้องเป็น 400 เมื่อ description ว่าง", () => {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
