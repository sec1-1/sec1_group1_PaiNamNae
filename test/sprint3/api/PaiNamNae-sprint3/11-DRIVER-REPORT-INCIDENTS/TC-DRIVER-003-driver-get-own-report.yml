info:
  name: TC-DRIVER-003-driver-get-own-report
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}reports/me"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code ต้องเป็น 200", () => {
          expect(res.getStatus()).to.equal(200);
        });


        test("Response ต้องเป็น array", () => {
          expect(res.getBody().data).to.be.an('array');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
