meta {
  name: 06Driver Approve Booking
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/api/bookings/{{bookingId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{driver_token}}
}

body:json {
  {
    "status": "CONFIRMED"
  }
}

tests {
  test("Approve booking: status 200", () => {
    expect(res.getStatus()).to.equal(200);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
